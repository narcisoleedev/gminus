/* caractere invalido */

int max(int v[], int length)
{  int i; int max#;
   i = 0;
   max# = 0;
   while (i < length)
   {  if (v[i] > max#) max# = v[i];
      i = i + 1;
   }
   return max#;
}

void main(void)
{  int i; int length;
   i = 0;
   length = input();
   if (length >= 0)
   {  int v[length];
      while (i < length)
      {  v[i] = input();
         i = i + 1;
      }
   }
   output(max(v, length));
}